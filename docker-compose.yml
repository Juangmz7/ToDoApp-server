
services:
  app:
    build: .
    ports:
      - "8081:8081"
    networks:
      - my-network

  postgres:
    image: postgres:latest
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: jugoba389320
      POSTGRES_DB: ToDoApp
    ports:
      - 5432:5432
    networks:
      - my-network

networks:
  my-network:
    driver: bridge
