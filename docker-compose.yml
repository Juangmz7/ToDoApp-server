
services:
  app:
    build: .
    environment:
      - DB_URL=jdbc:postgresql://pgvector:5432/ToDoApp # Intern URL connection
      - DB_USERNAME=${DB_USERNAME}
      - DB_PASSWORD=${DB_PASSWORD}
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    ports:
      - "8081:8081"
    depends_on:
      - pgvector
      - redis
    networks:
      - my-network

  redis:
    image: redis:7.2
    ports:
      - "6379:6379"
    networks:
      - my-network


  pgvector:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ToDoApp
    labels:
      - "org.springframework.boot.service-connection=postgres"
    ports:
      - 5433:5432
    networks:
      - my-network

networks:
  my-network:
    driver: bridge

